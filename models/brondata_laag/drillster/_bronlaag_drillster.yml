version: 2

models:
  - name: drillster_group_objectives
    description: "Consumption group objectives (work in progress)"
    columns:
      - name: id
      - name: group_name
      - name: objective
